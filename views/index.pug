html
  head
    title= title
  body
    h1 Submit a JSON Object
    form(action="/", method="POST")
      input(type="text", name="name")
      button(type="submit") Submit
    p List of people
    ul  
      each val in judges
        li= val